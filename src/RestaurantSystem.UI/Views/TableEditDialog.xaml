<Window x:Class="RestaurantSystem.UI.Views.TableEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Table" 
        Height="500" 
        Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" 
                Background="#673AB7"
                Padding="24">
            <StackPanel>
                <TextBlock Text="{Binding Title}" 
                          FontSize="24"
                          FontWeight="SemiBold"
                          Foreground="White"/>
                <TextBlock Text="Fill in the table details below"
                          FontSize="13"
                          Foreground="White"
                          Opacity="0.9"
                          Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" 
                     VerticalScrollBarVisibility="Auto"
                     Padding="24">
            <StackPanel>
                <!-- Table Name -->
                <TextBlock Text="Table Name *" 
                          FontSize="13"
                          FontWeight="SemiBold"
                          Margin="0,0,0,8"/>
                <TextBox x:Name="NameTextBox"
                        Text="{Binding TableName, UpdateSourceTrigger=PropertyChanged}"
                        materialDesign:HintAssist.Hint="e.g. Table 1, VIP Table..."
                        Height="48"
                        Padding="12,0"
                        FontSize="14"
                        BorderBrush="#E0E0E0"
                        BorderThickness="1"
                        Margin="0,0,0,20"/>

                <!-- Location -->
                <TextBlock Text="Location *" 
                          FontSize="13"
                          FontWeight="SemiBold"
                          Margin="0,0,0,8"/>
                <ComboBox x:Name="LocationComboBox"
                         SelectedValue="{Binding Location}"
                         materialDesign:HintAssist.Hint="Select location..."
                         Height="48"
                         Padding="12,0"
                         FontSize="14"
                         BorderBrush="#E0E0E0"
                         BorderThickness="1"
                         Margin="0,0,0,20">
                    <ComboBoxItem Content="Main Hall" Tag="MainHall"/>
                    <ComboBoxItem Content="Terrace" Tag="Terrace"/>
                    <ComboBoxItem Content="Window" Tag="Window"/>
                    <ComboBoxItem Content="VIP Room" Tag="VIPRoom"/>
                    <ComboBoxItem Content="Bar" Tag="Bar"/>
                </ComboBox>

                <!-- Seats Count -->
                <TextBlock Text="Seats Count *" 
                          FontSize="13"
                          FontWeight="SemiBold"
                          Margin="0,0,0,8"/>
                <TextBox x:Name="SeatsTextBox"
                        Text="{Binding SeatsCount, UpdateSourceTrigger=PropertyChanged}"
                        materialDesign:HintAssist.Hint="Number of seats"
                        Height="48"
                        Padding="12,0"
                        FontSize="14"
                        BorderBrush="#E0E0E0"
                        BorderThickness="1"
                        Margin="0,0,0,20"/>

                <!-- Is Active -->
                <CheckBox x:Name="IsActiveCheckBox"
                         IsChecked="{Binding IsTableActive, Mode=TwoWay}"
                         Content="Table is active"
                         FontSize="14"
                         Margin="0,8,0,0"/>

                <TextBlock Text="* Required fields"
                          FontSize="12"
                          Foreground="#999"
                          Margin="0,20,0,0"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Footer -->
        <Border Grid.Row="2" 
                Background="#F5F5F5"
                BorderBrush="#E0E0E0"
                BorderThickness="0,1,0,0"
                Padding="24,16">
            <StackPanel Orientation="Horizontal" 
                       HorizontalAlignment="Right">
                <Button Content="CANCEL"
                       Click="CancelButton_Click"
                       Height="42"
                       Padding="24,0"
                       Margin="0,0,12,0"
                       Background="White"
                       Foreground="#666"
                       BorderBrush="#E0E0E0"
                       BorderThickness="1"
                       FontWeight="SemiBold"
                       FontSize="13"
                       Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
                <Button Content="SAVE"
                       Click="SaveButton_Click"
                       Height="42"
                       Padding="32,0"
                       Background="#673AB7"
                       Foreground="White"
                       BorderThickness="0"
                       FontWeight="SemiBold"
                       FontSize="13"
                       Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>

