<UserControl x:Class="RestaurantSystem.UI.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:RestaurantSystem.UI.ViewModels.Menu"
             d:DataContext="{d:DesignInstance Type=vm:MenuViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Menu Management" 
                       Style="{StaticResource H1}"
                       DockPanel.Dock="Left"/>
            
            <StackPanel Orientation="Horizontal" 
                       DockPanel.Dock="Right"
                       HorizontalAlignment="Right">
                <Button Content="ADD DISH" 
                        Style="{StaticResource PrimaryButton}"
                        Margin="0,0,8,0"/>
                <Button Content="REFRESH"
                        Style="{StaticResource SecondaryButton}"/>
            </StackPanel>
        </DockPanel>

        <!-- Filter Panel -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBox materialDesign:HintAssist.Hint="Search dishes..."
                     Width="200"
                     Margin="0,0,8,0"/>
            
            <Button Content="SEARCH"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Margin="0,0,8,0"/>

            <ComboBox materialDesign:HintAssist.Hint="Filter by category"
                     Width="150"
                     Margin="0,0,8,0"/>
        </StackPanel>

        <!-- Dishes List -->
        <materialDesign:Card Grid.Row="2">
            <DataGrid AutoGenerateColumns="False"
                     CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="Auto"/>
                    <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat='{}{0:C}'}" Width="Auto"/>
                    <DataGridTextColumn Header="Cooking Time" Binding="{Binding CookingTimeMinutes}" Width="Auto"/>
                    <DataGridCheckBoxColumn Header="Available" Binding="{Binding IsAvailable}" Width="Auto"/>
                    <DataGridTemplateColumn Header="Actions" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Edit"
                                           Style="{StaticResource MaterialDesignFlatButton}"
                                           Margin="4,0"/>
                                    <Button Content="Delete"
                                           Style="{StaticResource MaterialDesignFlatButton}"
                                           Margin="4,0"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>
    </Grid>
</UserControl>

