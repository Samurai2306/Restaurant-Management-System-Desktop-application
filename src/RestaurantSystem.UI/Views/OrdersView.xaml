<UserControl x:Class="RestaurantSystem.UI.Views.OrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:RestaurantSystem.UI.ViewModels.Orders"
             d:DataContext="{d:DesignInstance Type=vm:OrdersViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Orders" 
                       Style="{StaticResource H1}"
                       DockPanel.Dock="Left"/>
            
            <StackPanel Orientation="Horizontal" 
                       DockPanel.Dock="Right"
                       HorizontalAlignment="Right">
                <Button Content="NEW ORDER" 
                        Style="{StaticResource PrimaryButton}"
                        Margin="0,0,8,0"/>
                <Button Content="REFRESH"
                        Style="{StaticResource SecondaryButton}"/>
            </StackPanel>
        </DockPanel>

        <!-- Filter Panel -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <ComboBox materialDesign:HintAssist.Hint="Filter by status"
                     Width="150"
                     Margin="0,0,8,0"/>
            
            <ComboBox materialDesign:HintAssist.Hint="Filter by table"
                     Width="150"
                     Margin="0,0,8,0"/>
        </StackPanel>

        <!-- Orders List -->
        <materialDesign:Card Grid.Row="2">
            <DataGrid AutoGenerateColumns="False"
                     CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Order #" Binding="{Binding Id}" Width="Auto"/>
                    <DataGridTextColumn Header="Table" Binding="{Binding TableName}" Width="Auto"/>
                    <DataGridTextColumn Header="Created" Binding="{Binding CreatedTime, StringFormat='{}{0:HH:mm}'}" Width="Auto"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="Auto"/>
                    <DataGridTextColumn Header="Items" Binding="{Binding ItemsCount}" Width="Auto"/>
                    <DataGridTextColumn Header="Total" Binding="{Binding TotalAmount, StringFormat='{}{0:C}'}" Width="Auto"/>
                    <DataGridTemplateColumn Header="Actions" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="View"
                                           Style="{StaticResource MaterialDesignFlatButton}"
                                           Margin="4,0"/>
                                    <Button Content="Close"
                                           Style="{StaticResource MaterialDesignFlatButton}"
                                           Margin="4,0"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>
    </Grid>
</UserControl>

